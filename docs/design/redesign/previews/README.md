# Vibemis redesign — static preview renders

Rendered screenshots of each redesigned screen (from `../Vibemis Redesign.dc.html`), 1920×1200.
Use these as a quick visual reference; the design file itself is the source of truth for exact
spacing, color, and states.

```
previews/
├─ 1a-computers.png           Host list — online/offline cards, host-type badges, Add card
├─ 1b-app-grid.png            App tiles — Desktop / Steam (focused, RESUME) / Virtual Desktop
├─ 1c-add-pc-dialog.png       Add-a-computer modal over scrim
├─ 1d-host-options-sheet.png  Right side-sheet (replaces the old context menu)
├─ 1e-settings.png            Sidebar categories + Video panel
└─ 1f-help.png                Quick Menu chord + gamepad/keyboard shortcuts + remote play
```

Before/after: compare against `../original-ui/` (screenshots of the current app).
